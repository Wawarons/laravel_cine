
    <nav id="header-navbar">
        <div id="header-navbar-links">
            <a href="{{route('home')}}">Home</a>
            <a href="{{route('home')}}">Movies</a>
            <a href="{{route('home')}}">Series</a>
            <a href="{{route('home')}}">Anime</a>
        </div>
        <div>
            <a href="{{ route('register') }}">
                <img src="{{ asset('assets/images/account.svg') }}" alt="login" width="35" height="35">
            </a>

        </div>
    </nav>
